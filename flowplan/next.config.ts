import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // typescript: {
  //   // !! אזהרה: זה מאפשר ל-Build להסתיים גם אם יש שגיאות TypeScript !!
  //   ignoreBuildErrors: true,
  // },
  // eslint: {
  //   // כדאי גם להתעלם מ-ESLint כדי שלא יכשיל את ה-Build על אזהרות
  //   ignoreDuringBuilds: true,
  // },
};

export default nextConfig;
